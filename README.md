🧠 Twitter Sentiment Classification using RNN

This project implements a Recurrent Neural Network (RNN) to classify the sentiment of tweets as Positive, Negative, or Neutral.
It uses deep learning techniques to understand the sequence structure of text and extract emotional intent from real-world Twitter data.

📌 Features

✔ Preprocessing of raw Twitter text (URLs, mentions, symbols, emojis)

✔ Tokenization & sequence padding

✔ Embedding layer for dense text representation

✔ RNN model (Simple RNN / LSTM / GRU — depending on your implementation)

✔ Trains a deep learning model for sentiment prediction

✔ Evaluation with accuracy, confusion matrix, and visualizations

✔ Option to test custom tweets
